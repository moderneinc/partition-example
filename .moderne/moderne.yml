build:
  partitions:
    - name: partition1
      steps:
        - type: resource
          inclusion: |-
            partition1.txt
    - name: partition2
      steps:
        - type: resource
          inclusion: |-
            partition2.txt
    - name: all
      steps:
        - type: resource
          inclusion: |-
            !partition*.*
            *
